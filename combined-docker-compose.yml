version: '3'

services:
  # API Service
  twitter-api:
    build: ./api
    container_name: twitter-sentiment-api
    environment:
      - PORT=5001
      - GOOGLE_APPLICATION_CREDENTIALS=/app/gcp-credentials.json
    ports:
      - "5001:5001"
    volumes:
      - ./api/gcp-credentials.json:/app/gcp-credentials.json:ro
    networks:
      - sentiment-network

  # Client UI
  twitter-ui:
    build: ./client
    container_name: twitter-sentiment-ui
    ports:
      - "80:80"
    volumes:
      - ./client/uploads:/app/uploads
      - ./client/results:/app/results
    environment:
      - API_BASE_URL=http://twitter-api:5001
    depends_on:
      - twitter-api
    networks:
      - sentiment-network

networks:
  sentiment-network:
    driver: bridge